diff --git a/src/controllers/AdminController.ts b/src/controllers/AdminController.ts
index 733e85b..9d04a8b 100644
--- a/src/controllers/AdminController.ts
+++ b/src/controllers/AdminController.ts
@@ -41,9 +41,9 @@ class AdminController {
                         select: { id: true, username: true, wallet_address: true }
                     },
                     _count: {
-                        select: { 
+                        select: {
                             positions: true,
-                            transactions: true 
+                            transactions: true
                         }
                     }
                 },
@@ -69,14 +69,15 @@ class AdminController {
 
     async createMarket(req: Request, res: Response) {
         try {
-            const { 
-                question, 
-                description, 
-                category, 
+            const {
+                question,
+                description,
+                category,
                 end_time,
                 oracle_source,
                 oracle_config,
-                resolution_criteria
+                resolution_criteria,
+                featured
             } = req.body;
 
             // validate required fields
@@ -118,7 +119,8 @@ class AdminController {
                     oracle_config,
                     resolution_criteria,
                     creator_id: req.user.sub,
-                    status: 'ACTIVE'
+                    status: 'ACTIVE',
+                    featured
                 }
             });
 
@@ -141,15 +143,16 @@ class AdminController {
     async updateMarket(req: Request, res: Response) {
         try {
             const { id } = req.params;
-            const { 
-                question, 
-                description, 
-                category, 
+            const {
+                question,
+                description,
+                category,
                 end_time,
                 oracle_source,
                 oracle_config,
                 resolution_criteria,
-                status
+                status,
+                featured
             } = req.body;
 
             // check if user has admin privileges (kyc_level >= 3)
@@ -194,7 +197,7 @@ class AdminController {
             if (oracle_config) updateData.oracle_config = oracle_config;
             if (resolution_criteria) updateData.resolution_criteria = resolution_criteria;
             if (status) updateData.status = status;
-
+            if (featured !== undefined) updateData.featured = featured;
             const market = await prisma.market.update({
                 where: { id },
                 data: updateData
